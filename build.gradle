buildscript {
    dependencies {
        // Enlaza el compilador de Android sin duplicar repositorios
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

// ❌ Se eliminó el bloque 'allprojects' que causaba el conflicto

